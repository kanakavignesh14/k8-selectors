apiVersion: v1
kind: Pod
metadata:
  name: anti-affinity
spec:
  affinity:
    nodeAffinity:
      # hard rule
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: topology.kubernetes.io/zone
            operator: NotIn   # here need to use NotIn so it will not go to tht node
            values:
            - us-east-1b
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: Always